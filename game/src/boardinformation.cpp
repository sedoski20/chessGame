#include "boardinformation.h"

BoardInformation::BoardInformation(const std::list<PieceInfo> &currentPlayerInformation,
                                   const std::list<PieceInfo> &opponentPlayerInformation)
                                   : currentPlayerInformation(),
                                    opponentPlayerInformation(){}
